let userProfile = {
    `username`: "Tevin Pilgrim-Hampden";
    `profilePicture`:"Software Engineer";
    `jobTitle`:"./assets/images/headshot.jpg";
}

const loadUserProfile => (userProfile)

.onload = function() {
    loadUserProfile(userProfile, onload);
  };

let createPost() = (onclick) => {
    
    const newInfo = document.createTextNode

}